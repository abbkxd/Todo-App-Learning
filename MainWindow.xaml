<Window x:Class="Todo_App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Todo_App"
        mc:Ignorable="d"
        WindowState="Maximized"
        WindowStartupLocation="CenterScreen"
        Title="ToDo-ist" Height="450" Width="400">

    <Window.Resources>
        
        <Style TargetType="Button">
            <Setter Property="Margin" Value="2.5"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="Height" Value="25"/>
            <Setter Property="Width" Value="100"/>

            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Trigger.EnterActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="Height" Duration="0:0:0.3"  To="40"/>
                                <DoubleAnimation Storyboard.TargetProperty="Width" Duration="0:0:0.3"  To="110"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.EnterActions>
                    <Trigger.ExitActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="Height" Duration="0:0:0.3"  To="25"/>
                                <DoubleAnimation Storyboard.TargetProperty="Width" Duration="0:0:0.3"  To="100"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.ExitActions>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="7*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="9*"/>
        </Grid.RowDefinitions>

        <!--List-View-->
        <Grid Grid.Row="1" Grid.Column="1">
            <ListView ItemsSource="{Binding Tasks}" SelectedItem="{Binding SelectedTask}">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Event" DisplayMemberBinding="{Binding eventHead}"
                                         Width="auto"/>
                        <GridViewColumn Header="Description" DisplayMemberBinding="{Binding eventDescp}"
                                         Width="auto"/>
                        <GridViewColumn Header="Date" DisplayMemberBinding="{Binding eventDate}"
                                        Width="auto"/>
                        <GridViewColumn Header="Status" DisplayMemberBinding="{Binding eventStatus}"
                Width="auto"/>
                    </GridView>
                </ListView.View>
            </ListView>

        </Grid>

        <!--Buttons-->
        <Grid Grid.Row="1">
            <StackPanel Orientation="Vertical" Margin="5">

                <Button Content="Add Event" Command="{Binding addTaskCommand}"/>
                <Button Content="Delete Event" Command="{Binding deleteTaskCommand}"/>
                <Button Content="Clear Events" Command="{Binding clearTaskCommand}" />
                <Button Content="Update Events" Command="{Binding updateTaskCommand}" />
                <Button Content="MarkCompleted(N/A)" Command="{Binding MarkTaskCommand}" />

            </StackPanel>
            
        </Grid>

    </Grid>
</Window>
